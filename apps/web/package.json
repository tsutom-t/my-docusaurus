{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "check-types": "tsc --noEmit",
    "deploy": "npm run build && mkdir -p ../../docs && rm -rf ../../docs/* && cp -r out/* ../../docs/"
  },
  "dependencies": {
    "next": "^14.2.20",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}